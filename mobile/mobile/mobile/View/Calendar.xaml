<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:mobile.ViewModel"
             x:Class="mobile.Pages.Calendar"
             xmlns:syncfusion="clr-namespace:Syncfusion.SfCalendar.XForms;assembly=Syncfusion.SfCalendar.XForms"
             Title="Calendar"
             BackgroundColor="White">


    <syncfusion:SfCalendar x:Name="calendar" ShowInlineEvents="True" InlineViewMode="Agenda" MaximumEventIndicatorCount="1" DataSource="{Binding CalendarInlineEvents}">>
        <syncfusion:SfCalendar.BindingContext>
            <vm:CalendarViewModel/>
        </syncfusion:SfCalendar.BindingContext>
        <syncfusion:SfCalendar.MonthViewSettings>
            <syncfusion:MonthViewSettings
                    DateSelectionColor="{StaticResource primary}"
                    TodaySelectionBackgroundColor="{StaticResource secondary}"
                    HeaderFontFamily="source"
                    DayHeaderFontFamily="source">

            </syncfusion:MonthViewSettings>
        </syncfusion:SfCalendar.MonthViewSettings>
    </syncfusion:SfCalendar>

</ContentPage>